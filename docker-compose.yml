version: '3'

services:
  server:
    build: ./
    ports:
      - '3000:3000'
    links:
      - redis
    environment:
      - REDIS_URL=redis://cache
  redis:
    image: redis:latest
    container_name: cache
    expose:
      - '6379'
